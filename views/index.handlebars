{{!-- <div class="box">
    <div class="content">
        <h1 class="title">
            Read the Latest from 'The Verge' in Tech
        </h1>
        <br>
        <p>Click 'Scrape' to get the latest articles</p>
    </div>
    <div class="columns">
        {{#each artricle}}
        <header class="card-header">
            <p class="card-header-title">
                {{this.title}}
            </p>
        </header>
        <div class="card-content">
            <div class="content">
                <a href="/readArticle/{{this._id}}" class="" id="btnSignUp">Read Article</a>
            </div>
        </div>
        {{/each}}
    </div>
</div> --}}
<section class="hero is-medium is-warning is-bold">
    <div class="hero-body">
        <div class="container">
            <h1 class="title">Read the Latest from 'The Verge' in Tech</h1>
            <h2 class="subtitle">Click 'Scrape' to get the latest articles</h2>
            <p class="control"><a class="button is-dark is-outlined is-rounded" href="/scrape"><span>Scrape</span></a></p>
        </div>
    </div>
</section>

<div class="columns">
    <div class=""></div>
    <div class="column s8 left-align">
        {{#each article}}
        <div class="card-content">
            <h4>{{this.title}}</h4>
            {{!-- goes to article.handlebars --}}
            <a href="/readArticle/{{this._id}}" class="" id="btnSignUp">Read Article</a>
        </div>
        {{/each}}
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.1.1.min.js"></script>